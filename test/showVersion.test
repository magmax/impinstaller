# -*- coding:utf-8; tab-width:4; mode:python -*-

const = load ('test/constants.py')

t1 = Test ( const.MAINEXE + ' -v' )
t1.post += FileContains(['version', 'ImpInstaller', 'Miguel Angel Garcia'])

t2 = Test ( const.MAINEXE + ' -h' )
t2.post += FileContains(['show this help message and exit',
                         'impinstaller.py',
                         'show program version and exit'])

t3 = Test ( const.MAINEXE, expected=2 )
